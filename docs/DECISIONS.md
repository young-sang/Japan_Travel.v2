# 의사결정 기록

이 프로젝트에서 **왜 그렇게 하기로 했는지**를 모아둔다. 무엇을 만들었는지는 코드와
설계 문서가 말해주지만, 왜 다른 길을 안 갔는지는 어디에도 남지 않는다. 그걸 남긴다.

## 쓰는 법

- 결정이 내려질 때마다 **맨 아래에 추가**한다. 번호는 이어서 붙이고 줄이지 않는다.
- 결정이 **뒤집히면 지우지 않는다.** 원래 항목의 상태를 `번복됨`으로 바꾸고 새 번호를
  가리킨다. 번복 자체가 기록할 값어치가 있다.
- **기각한 대안을 반드시 쓴다.** 나중에 같은 고민이 돌아왔을 때 이미 검토했다는 걸
  아는 것이 이 문서의 절반이다.
- 한 항목은 짧게. 길어지면 `docs/superpowers/specs/` 에 문서를 만들고 여기서 링크한다.

| 상태 | 뜻 |
|---|---|
| `확정` | 지금 유효하다 |
| `보류` | 결정을 미뤘다. 언제 정할지 적어둔다 |
| `번복됨` | 더 이상 유효하지 않다. 대체 항목 번호를 적는다 |

---

## 색인

| # | 결정 | 날짜 | 상태 |
|---|---|---|---|
| [D-001](#d-001--리팩토링이-아니라-도메인별-신규-작성) | 리팩토링이 아니라 도메인별 신규 작성 | 2026-09-15 | 확정 |
| [D-002](#d-002--maven--gradle-jdbctemplate--jpa) | Maven → Gradle, JdbcTemplate → JPA | 2026-09-15 | 확정 |
| [D-003](#d-003--코드는-사용자가-직접-쓴다) | 코드는 사용자가 직접 쓴다 | 2026-09-15 | 확정 |
| [D-004](#d-004--api-계약과-스키마를-고정한다) | API 계약과 스키마를 고정한다 | 2026-09-15 | 번복됨 → D-008 |
| [D-005](#d-005--예외--상태코드-번역기를-한-곳에-둔다) | 예외 → 상태코드 번역기를 한 곳에 둔다 | 2026-09-17 | 확정 |
| [D-006](#d-006--명세서가-아니라-가이드-형식으로-진행한다) | 명세서가 아니라 가이드 형식으로 진행한다 | 2026-09-17 | 확정 |
| [D-007](#d-007--도메인-단위-그-안에서-계층-단위로-끊는다) | 도메인 단위, 그 안에서 계층 단위로 끊는다 | 2026-09-17 | 확정 |
| [D-008](#d-008--프론트를-백엔드에-맞춘다-전제-3개-해제) | 프론트를 백엔드에 맞춘다 (전제 3개 해제) | 2026-09-17 | 확정 |
| [D-009](#d-009--1차-범위에서-관리자수집기감사로그모니터링을-뺀다) | 1차 범위에서 관리자·수집기·감사로그·모니터링을 뺀다 | 2026-09-17 | 확정 |
| [D-010](#d-010--destinations--festivals--places-한-테이블) | destinations + festivals → places 한 테이블 | 2026-09-17 | 확정 |
| [D-011](#d-011--활동-대상을-places-로-한정해-다형성을-없앤다) | 활동 대상을 places 로 한정해 다형성을 없앤다 | 2026-09-17 | 확정 |
| [D-012](#d-012--태그와-타임라인을-행으로-정규화한다) | 태그와 타임라인을 행으로 정규화한다 | 2026-09-17 | 확정 |
| [D-013](#d-013--리뷰-중복을-허용한다) | 리뷰 중복을 허용한다 | 2026-09-17 | 확정 |
| [D-014](#d-014--옛-백엔드를-전환-없이-통째로-지운다) | 옛 백엔드를 전환 없이 통째로 지운다 | 2026-09-17 | 확정 |
| [D-015](#d-015--옛-db-에서-변환해-시드한다) | 옛 db 에서 변환해 시드한다 | 2026-09-17 | 확정 |
| [D-016](#d-016--baseline-diff-를-스모크-스크립트로-대체한다) | baseline diff 를 스모크 스크립트로 대체한다 | 2026-09-17 | 확정 |
| [D-017](#d-017--날짜-타입api-경로prefecture-제약은-task-1-에서-정한다) | 날짜 타입·API 경로·prefecture 제약은 Task 1 에서 정한다 | 2026-09-17 | 보류 |

---

### D-001 · 리팩토링이 아니라 도메인별 신규 작성

**2026-09-15 · 확정**

옛 코드를 옮기는 대신 `_repo/` 아래에 도메인별로 새로 짓기로 했다.

**이유** — 1순위 목표가 구조 개선이 아니라 **코드 이해**다. 남의 코드를 옮기면
옮기는 법만 배우고, 새로 지으면 왜 그 구조인지를 배운다.

**기각** — 기존 코드를 계층별로 이동시키는 리팩토링 계획 (문서 2건 폐기).

---

### D-002 · Maven → Gradle, JdbcTemplate → JPA

**2026-09-15 · 확정** · 커밋 `7b56062`

**이유** — JPA 를 쓰기로 했고, Spring Boot 3.3.4 는 Gradle 9 를 지원하지 않아
Gradle 8.14.5 (Groovy DSL) 로 고정했다.

**영향** — `ddl-auto: none` · `open-in-view: false`. 스키마의 주인은 `schema.sql` 이고
Hibernate 는 스키마를 건드리지 않는다. SQLite 는 공식 dialect 가 없어
`hibernate-community-dialects` 를 쓴다 — 예상 밖 동작이 나오면 **그 지점만**
`nativeQuery = true` 로 우회한다.

---

### D-003 · 코드는 사용자가 직접 쓴다

**2026-09-15 · 확정**

Claude 는 가이드·검증·리뷰를 맡는다. 구현 코드는 사용자가 막혔을 때만 제공한다.

**이유** — D-001 과 같다. 목표가 이해라면 코드를 받는 것으로는 달성되지 않는다.

---

### D-004 · API 계약과 스키마를 고정한다

**2026-09-15 · 번복됨 → [D-008](#d-008--프론트를-백엔드에-맞춘다-전제-3개-해제)**

프론트를 한 줄도 고치지 않기 위해 API 경로·응답 필드명·상태코드·`schema.sql` 을
고정 대상으로 두었다. `git diff --stat frontend/` 가 비어 있는 것이 계약 유지의
증거였다.

**왜 뒤집혔나** — 스키마를 고정하면 정작 고쳐야 할 문제(FK 부재, JSON 컬럼,
다형성)를 건드릴 수 없다. 2026-09-17 에 프론트도 나중에 맞추기로 하면서 해제됐다.

---

### D-005 · 예외 → 상태코드 번역기를 한 곳에 둔다

**2026-09-17 · 확정**

Service 가 `NotFoundException` · `ForbiddenException` · `ConflictException` 을 던지고,
`ApiExceptionHandler` 하나가 404 · 403 · 409 로 번역한다.

**이유** — 컨트롤러에서 `if (empty) return notFound()` 를 하면 같은 판단이 일반
컨트롤러와 관리자 컨트롤러 양쪽에 생긴다. 규칙이 두 군데로 갈라진다.

**세부** — `ConflictException` 은 선택적 `code` 와 `extra` 를 들고 다니며 `body()` 로
응답 맵을 **스스로** 조립한다. 핸들러가 조립하면 거기에 `if (code != null)` 이 생기기
때문이다. **핸들러에 `if` 나 `instanceof` 가 들어가면 설계가 깨진 것이다.**

**금지** — `@ExceptionHandler(Exception.class)` catch-all. `ResponseStatusException` 을
가로채 상태코드를 바꿔버린다.

---

### D-006 · 명세서가 아니라 가이드 형식으로 진행한다

**2026-09-17 · 확정**

표로 정리된 명세를 던지는 대신, **왜 이렇게 하는가 → 무엇을 쓸 것인가 → 직접 작성
→ 확인** 순으로 대화하며 간다.

**이유** — 사용자 요청. 408줄짜리 Task 1 명세서는 정확했지만, 읽고 옮겨 적는 일이
되어 D-003 의 목적과 어긋났다.

---

### D-007 · 도메인 단위, 그 안에서 계층 단위로 끊는다

**2026-09-17 · 확정**

한 번에 한 도메인. 그 안에서 `entity → repository → service → dtos → controller`
순서로 한 계층씩. 계층마다 가이드 → 작성 → 확인 → 다음.

**기각** — 파일 하나씩 (너무 잘게 끊겨 흐름이 안 보임), Task 통짜 문서 (D-006 과 충돌).

---

### D-008 · 프론트를 백엔드에 맞춘다 (전제 3개 해제)

**2026-09-17 · 확정** · [D-004](#d-004--api-계약과-스키마를-고정한다) 를 대체

프론트엔드 · API 계약 · DB 스키마가 모두 자유로워졌다. 기존 앱은 **기능 명세서**
역할만 한다. 프론트는 백엔드가 굳은 뒤 마지막 Task 에서 맞춘다.

**전제가 된 사실** — 현재 DB 내용은 테스트용 더미다. 옮길 실데이터가 없으면
스키마를 갈아엎는 비용이 거의 0이다. 따라서 **지금 완벽한 설계를 할 필요가 없고,
만들다가 잘못을 발견하면 바꾼다.**

**대가** — 회귀 안전망이던 `baseline/` diff 가 무효가 됐다 ([D-016](#d-016--baseline-diff-를-스모크-스크립트로-대체한다)).

**영향** — 설계 문서 2건 폐기, [2026-09-17-greenfield-rebuild-design.md](superpowers/specs/2026-09-17-greenfield-rebuild-design.md) 로 대체.

---

### D-009 · 1차 범위에서 관리자·수집기·감사로그·모니터링을 뺀다

**2026-09-17 · 확정**

도메인 14 → 10, 엔드포인트 약 55 → 30.

**뺀 것** — 관리자 화면·API 전부 / Wikipedia 수집기 (858줄) / 수집 모니터링
(`bulk_runs` · `collector_runs` · `ZombieWatcher` · `BulkProgress` 360줄 ·
`CollectionStatus` 470줄) / 감사 로그 / 캐시 창구 / 대시보드 통계 /
`collections` · `collection_items` · `achievements` (0행, 자바 코드가 참조하지 않음).

**이유** — 모니터링은 프로젝트 `CLAUDE.md` 의 기본 제외 항목이다. 감사 로그는 10개
도메인 전부의 Service 에 한 줄씩 들어가 결합을 만든다. 관리자 기능은 나중에 필요한
것만 추려서 다시 만든다 — **영구 폐기가 아니라 범위 축소다.**

**남긴 것** — `users.role` 컬럼. 나중에 필요하고 컬럼 하나는 비용이 아니다.
1차에서는 권한 분기를 쓰지 않는다.

**주의** — 수집기를 빼면 `places` 를 채울 방법이 시드뿐이다 ([D-015](#d-015--옛-db-에서-변환해-시드한다)).

---

### D-010 · destinations + festivals → places 한 테이블

**2026-09-17 · 확정**

`kind` 컬럼(`'destination'` | `'festival'`)으로 구분한다.

**이유** — 두 테이블의 컬럼이 거의 같았다. `name` · `prefecture` · `lat` · `lng` ·
`image_path` · `description` · `wiki_title` · `last_refreshed_at` 이 동일하고 축제에만
`month` · `date_text` 가 더 있었다. 사실상 같은 테이블이었다.

**대가** — `month` · `date_text` 에 `NOT NULL` 을 걸 수 없다. 관광지 행에서는 NULL 이다.
반대 방향은 `CHECK (kind <> 'destination' OR (month IS NULL AND date_text IS NULL))`
로 막는다.

---

### D-011 · 활동 대상을 places 로 한정해 다형성을 없앤다

**2026-09-17 · 확정**

즐겨찾기 · 리뷰 · 방문기록은 `places` 만 가리킨다. 코스는 대상이 아니다.

**문제** — 옛 스키마는 `target_type` + `target_id` 로 "관광지·축제·코스 중 아무거나"를
가리켰다. FK 컬럼 하나는 테이블 하나만 가리킬 수 있으므로 FK 를 걸 수 없었고,
그래서 관광지를 지울 때 자바가 손으로 `favorites` · `reviews` · `history` 를 지우고
있었다 — **DB 가 할 일을 자바가 대신했다.**

**검토하고 기각한 대안**

| 안 | 내용 | 기각 이유 |
|---|---|---|
| A 레지스트리 | `content` 상위 테이블 + JOINED 상속 | DB 자체가 처음인데 **이해 안 되는 구조 위에 10개 도메인을 쌓게 된다.** 목표(코드 이해)와 정반대 |
| B nullable FK | `favorites(place_id, course_id)` + CHECK | 컬럼 중 하나는 항상 NULL. 대상이 늘면 세 테이블에 컬럼 추가 |
| C 테이블 분리 | `favorites` + `course_favorites` … | 테이블 6~9개, 합친 목록 조회가 UNION |
| D 단일 테이블 | 셋을 한 테이블에 | `NOT NULL` 제약을 거의 못 검 |
| E 트리거 | 지금 모양 + DELETE 트리거 | 로직이 자바 코드에 안 보인다 |

**왜 한정으로 풀었나** — 대상이 하나면 문제 자체가 사라진다. 평범한 FK 하나씩이고
CHECK 도 NULL 컬럼도 새 개념도 없다. 코스 즐겨찾기가 필요해지면 `course_favorites`
테이블을 **추가만** 하면 된다 — 기존 테이블을 한 줄도 건드리지 않는다.

> 근거로 쓰지 않은 것: "코스 활동 0건". 데이터가 더미이므로 사용량은 증거가 아니다.
> 이것은 **범위 축소 결정**이지 사용량 판단이 아니다.

---

### D-012 · 태그와 타임라인을 행으로 정규화한다

**2026-09-17 · 확정**

`tags TEXT` → `place_tags` · `course_tags`. `timeline_json TEXT` → `course_stops`.

**이유(태그)** — 검색이 `LIKE '%"온천"%'` 이었다. 행으로 빼면 `WHERE tag = '온천'` 이
되고 인덱스가 먹는다. JPA 쪽에서도 `AttributeConverter` 가 필요 없어진다.

**이유(타임라인)** — 옛 `timeline_json` 은 `title` · `desc` · `lat` · `lng` 를 복사해
두어 코스와 장소 사이에 링크가 없었다. 확인해 보니 `CourseBuilder.jsx` 는 정류장을
만들 때 항상 실제 장소에서 가져오고 `targetId` 를 이미 들고 있다. 따라서
`place_id NOT NULL` 참조만 하면 되고 표시용 값은 조인으로 얻는다.

**검토하고 기각** — 참조 + 스냅샷 컬럼 병행 (원본 삭제에 대비). 정류장이 항상 실제
장소에서 오는 것이 확인되어 불필요했다.

---

### D-013 · 리뷰 중복을 허용한다

**2026-09-17 · 확정**

`reviews` 에 `UNIQUE(user_id, place_id)` 를 걸지 않는다.

**이유** — 옛 `UserDataRepository` 가 중복 검사 없이 `INSERT` 한다. 한 사용자가 같은
장소에 리뷰를 여러 개 쓸 수 있고, 그 동작을 유지한다. 제약을 새로 거는 것은 기능
변경이다.

---

### D-014 · 옛 백엔드를 전환 없이 통째로 지운다

**2026-09-17 · 확정**

`git tag pre-greenfield` 를 찍고 옛 백엔드 코드를 삭제한다. `Legacy*` 이름변경도,
빈 이름 충돌 회피도 하지 않는다.

**이유** — 새 백엔드에 소비자가 없다 (프론트는 마지막에 맞춘다). 공존시킬 이유가
없으므로 [D-004](#d-004--api-계약과-스키마를-고정한다) 시절에 필요했던 전환 전략
전체가 불필요해졌다.

**영향** — `_repo` 임시 접두사도 존재 이유가 사라진다. `com.japantravel._repo.common`
→ `com.japantravel.common` 으로 옮긴다.

**보존** — `data/japan_travel.db` 원본은 지우지 않는다 ([D-015](#d-015--옛-db-에서-변환해-시드한다), 그리고 프로젝트
`CLAUDE.md` 의 "db 삭제 전 백업" 규칙).

---

### D-015 · 옛 db 에서 변환해 시드한다

**2026-09-17 · 확정**

`scripts/seed_from_old_db.py` (일회성) 로 옛 db 를 읽어 새 스키마로 변환한다.
**스키마 확정 직후** 만든다.

**이유** — 수집기가 1차 범위 밖이라 ([D-009](#d-009--1차-범위에서-관리자수집기감사로그모니터링을-뺀다))
`places` 를 채울 방법이 시드뿐이다. 개발 내내 빈 목록으로 작업하면 확인이 안 된다.
관광지 118건·축제 26건은 더미가 아니라 Wikipedia 에서 온 진짜 내용이라 쓸 값어치가
있다.

**기각** — 손으로 짠 소량 시드 (목록·필터·지도가 빈약해 보임), 수집기를 1차로 복귀
(858줄짜리 가장 큰 도메인이 돌아옴).

---

### D-016 · baseline diff 를 스모크 스크립트로 대체한다

**2026-09-17 · 확정**

`scripts/smoke/<domain>.sh` 를 도메인마다 하나씩 쌓고, `scripts/smoke/all.sh` 가
도메인을 끝낼 때마다 통과해야 한다.

**이유** — [D-008](#d-008--프론트를-백엔드에-맞춘다-전제-3개-해제) 로 응답 모양이
바뀌므로 `baseline/` 과 비교할 수 없다. 테스트 프레임워크는 도입하지 않기로 했으므로
curl 스크립트가 **유일한 회귀 안전망**이다. 새 도메인을 시작하기 전에 반드시 이전
것들이 여전히 도는지 확인한다.

**환경 함정 2개**
- Git Bash 에서 curl 에 한글을 그대로 쓰면 서버에 온전히 도달하지 않는다.
  퍼센트 인코딩이 필요하고, 응답 확인도 `PYTHONIOENCODING=utf-8` 을 붙인 Python 으로 한다.
- 옛 프로세스가 8080 을 잡고 있으면 새 앱이 죽었는데도 200 이 돌아온다.
  `netstat -ano | grep ":8080" | grep LISTEN` 으로 먼저 확인한다.

---

### D-017 · 날짜 타입·API 경로·prefecture 제약은 Task 1 에서 정한다

**2026-09-17 · 보류**

| 항목 | 선택지 | 언제 |
|---|---|---|
| 날짜 자바 타입 | `Instant` vs `String` | Task 1 — SQLite + 커뮤니티 dialect 가 실제로 어떻게 저장하는지 보고 확정 |
| API 경로 | `/api/places?kind=` 로 통합 vs `/api/destinations` · `/api/festivals` 유지 | Task 1 |
| `prefecture` | TEXT 그대로 vs `CHECK` 제약 vs 참조 테이블 | 필요해지면. 참조 테이블은 조인만 늘고 얻는 게 오타 방지뿐이라 보류 |

**이유** — 셋 다 실제로 만들어보고 정하는 편이 지금 추측하는 것보다 낫다. 특히
날짜는 SQLite 에 날짜 타입이 없고 커뮤니티 dialect 를 쓰는 조합이라 단언할 수 없다.
